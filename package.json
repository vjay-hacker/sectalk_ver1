{
  "name": "sectalk-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --recursive build",
    "test": "pnpm --recursive test"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
